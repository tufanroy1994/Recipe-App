import { StyleSheet } from 'react-native';
import { AppColors, wp } from '../../utils';

export const styles = StyleSheet.create({
  seperator: {
    borderBottomColor: AppColors.INPUT_BORDER,
    borderTopWidth: wp(2),
  },
});
